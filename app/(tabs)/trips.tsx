import { Text } from "@/components/text";
import { SafeAreaView, View } from "react-native";

const Trips = () => {
    return (
        <SafeAreaView>
            <Text>Trips</Text>
        </SafeAreaView>
    );
}
 
export default Trips;