import { Text } from "@/components/text";
import { SafeAreaView, View } from "react-native";

const Messages = () => {
    return (
        <SafeAreaView>
            <Text>Messages</Text>
        </SafeAreaView>
      );
}
 
export default Messages;